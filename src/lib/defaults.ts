// Profil par défaut de J.A.R.V.I.S. — modifiable ici, pendant l'installation (version PC),
// dans la configuration initiale, ou à tout moment dans Paramètres → Profil.
export const DEFAULT_USER_NAME = "Nathan";
/** "name" = JARVIS vous appelle par votre prénom ; "title" = par l'appellation (Monsieur, Madame…). */
export const DEFAULT_ADDRESS_BY: "name" | "title" = "name";
export const DEFAULT_HONORIFIC = "Monsieur";
